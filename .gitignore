build/
build-*/
cmake-build-*/
build-coverage/

# install(DESTINATION dist/...) output - the artifact this project hands to
# downstream SAPI projects (see root CLAUDE.md's "dist/" convention), not
# source; generate it with `cmake --install build --prefix .`.
dist/*
!dist/.gitkeep
*.o
*.obj
*.out
compile_commands.json
.cache/

# Doxygen output
docs/html/
docs/latex/
docs/api/

# OS / editor cruft
.DS_Store
.vscode/
*.swp

# Local Claude tooling (skill cache, not project source)
.claude/
examples/2oo2-redundant-cluster/2oo2-demo
*.dump
